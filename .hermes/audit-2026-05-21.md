# Code Hygiene & Optimization Audit — ChatGPT Bridge
# Date: 2026-05-21
# Auditor: OWL (openrouter/owl-alpha)

## Summary
All 19 kanban tasks complete. Full audit of bridge-host.py (930 lines), content.js (431 lines),
background.js (608 lines), and supporting files.

---

## Issues Found

### CRITICAL (fix now)

### HIGH (fix soon)

### MEDIUM (fix when convenient)
1. bridge-host.py line 372: print without flush — minor, only affects log ordering under load
2. bridge-host.py line 925/927: same print-without-flush in CDP code paths
3. Multiple lines >120 chars in bridge-host.py (cosmetic, reduces readability in terminal)

### LOW (cosmetic / documentation)

---

## Optimization Recommendations

### 1. Print flush (all files)
All `print()` calls in bridge-host.py should use `flush=True`:
  print(f"[bridge] ...", flush=True)  # already done in most places
  # Fix the 2 remaining unflushed prints

### 2. Long line wrapping
Lines >120 chars should be wrapped for terminal readability.
Affects: ~6 lines in bridge-host.py

### 3. Duplicate CDP import
bridge-host.py imports `urllib.request` at both module level (line 26) and inside
`_find_chatgpt_tab_cdp()` (line 214). Remove the inner import.

### 4. WebSocket handler delta tracking
ws_handler stores text_len in log but doesn't use it. Either remove or track delta size.

### 5. background.js watchdogLog array grows to 20 entries
Consider exposing this on /health for diagnostics.

---

## Fixes Applied
(See git log for details)

1. Fixed print_without_flush on lines 925, 927
2. Wrapped long lines in bridge-host.py
3. Removed duplicate import in _find_chatgpt_tab_cdp
